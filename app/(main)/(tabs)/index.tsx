import { ScreenPlaceholder } from '@/components/layout/ScreenPlaceholder';

export default function MeetingsScreen() {
  return (
    <ScreenPlaceholder title="Meetings" subtitle="Home tab — UI next" />
  );
}
