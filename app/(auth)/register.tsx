import { ScreenPlaceholder } from '@/components/layout/ScreenPlaceholder';

export default function RegisterScreen() {
  return <ScreenPlaceholder title="Register" subtitle="Initialize Account — UI next" />;
}
