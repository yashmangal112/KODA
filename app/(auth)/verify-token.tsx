import { ScreenPlaceholder } from '@/components/layout/ScreenPlaceholder';

export default function VerifyTokenScreen() {
  return <ScreenPlaceholder title="Verify Email" subtitle="OTP verification — UI next" />;
}
